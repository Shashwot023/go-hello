apiVersion: v1
kind: Service
metadata:
  name: go-hello
spec:
  type: {{ .Values.service.type }}

  selector:
    app: go-hello

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
